
@import url(http://fonts.googleapis.com/css?family=Montserrat:400|);


// Special font mixins
@mixin font($type: body) { 
  @if ($type == logo) {
    font-family: "Montserrat", sans-serif;
    font-size: $heading-level-1;
    
  }
  @if ($type == body) {
    font-family: "Europa", sans-serif;
    font-weight: 300;
    
  }
  @if ($type == title) {
    font-family: "Europa", sans-serif;
    font-weight: 400;
    font-size: $heading-level-1;
    
  }
  @if ($type == subtitle) {
    font-family: "Europa", sans-serif;
    font-weight: 300;
    font-style: italic;
    
  }
  @if ($type == services) {
    font-family: "Europa", sans-serif;
    font-weight: 400;
    
  }
}


// A list of all font families
$font-families: Montserrat, Europa;


// A list of all font mixins
$font-mixins: logo, body, title, subtitle, services;